{
  "name": "softtech-build",
  "version": "1.0.0",
  "private": true,
  "description": "Build pipeline — minification only",
  "scripts": {
    "build": "node build.js",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report tests/report"
  },
  "devDependencies": {
    "terser": "^5.31.0",
    "clean-css": "^5.3.3",
    "html-minifier-terser": "^7.2.0",
    "fs-extra": "^11.2.0",
    "glob": "^11.0.0",
    "@playwright/test": "^1.44.0",
    "http-server": "^14.1.1"
  }
}
